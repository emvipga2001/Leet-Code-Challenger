/**
 * @param {string} s
 * @param {number} numRows
 * @return {string}
 */
var convert = function(s, numRows) {
    let indexFE = numRows + numRows - 2;
    let res = new Array(s.length);

    for (let i = 0; i < s.length; i++) {
        
    }
};

console.log(convert(s = "PAYPALISHIRING", numRows = 3));
